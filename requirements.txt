# GitHub Issue Analyzer - Requirements

# Core dependencies
requests>=2.31.0

# Web UI (Flask)
Flask>=3.0.0

# MCP Server - SDK for creating tools that Bob can use
mcp>=0.9.0

# Note: Uses built-in Python libraries and GitHub CLI (gh) for GitHub operations
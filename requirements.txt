# GitHub Issue Analyzer - Minimal MVP Requirements

# MCP SDK for creating tools that Bob can use
mcp>=0.9.0

# No other dependencies needed!
# Uses built-in Python libraries and GitHub CLI (gh) for GitHub operations